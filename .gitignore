# Environment
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/

# Cloned upstream (re-created by scripts/setup_runpod.sh); the patch we apply IS tracked
third_party/RULER/

# Generated RULER sets (scripts/gen_data.sh, ~21 MB and reproducible from the seeds)
data/

# Run outputs are OUTPUT, JSON included: S0 writes its licence into runs/detector.json, and while that file was tracked
# every README, checkpoint and eval table from S0 on would have carried <hash>+dirty (git_hash() reads the whole tree)
# -- a provenance hash nobody can check out (review acda7b6 2a).  The tracked copies were removed from the index.
runs/
Claude outputs/

# Generated at handover (scripts/handoff.sh); the tarball goes to ~/marsea_<short>.tar.gz
HANDOFF.txt

# Tooling
shell scripts/
documents/


# Generated by scripts/setup_runpod.sh (the cache location for this pod)
pod_env.sh
